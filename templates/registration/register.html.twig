{% extends 'base.html.twig' %}

{% block title %}Premier administrateur{% endblock %}

{% block body %}
<section class="register_section">
    <h1 class="Veuillez saisir le premier administrateur"></h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
</section>
{% endblock %}
