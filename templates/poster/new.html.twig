{% extends 'base.html.twig' %}

{% block title %}New Poster{% endblock %}

{% block body %}
    <section class="poster_page_new text-white">
        <div class="poster_show_container container">
            <h1 class="poster_page_new_title">Ajouter une image</h1>
                {% for category, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ category }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            {{ include('poster/_form.html.twig') }}
            
            <a class="btn btn-light" href="{{ path('poster_index') }}">Retour</a>
            <a class="btn btn-light" href="{{ path('home_index') }}">Revenir à l'accueil</a>
            
        </div>
    </section>
{% endblock %}
