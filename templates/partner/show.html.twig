{% extends 'base.html.twig' %}

{% block title %}Partner{% endblock %}

{% block body %}
    <section class="event">

    <h1>Partner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner.name }}</td>
            </tr>
            <tr>
                <th>Decription</th>
                <td>{{ partner.decription }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ partner.poster }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_index') }}">back to list</a>

    <a href="{{ path('partner_edit', {'id': partner.id}) }}">edit</a>

    {{ include('partner/_delete_form.html.twig') }}
    </section>
{% endblock %}
