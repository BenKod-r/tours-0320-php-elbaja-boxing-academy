{% extends 'base.html.twig' %}

{% block title %}New Partner's Poster{% endblock %}

{% block body %}
    <section class="poster_page_new text-white">
        <div class="poster_show_container container">
            <h1 class="poster_page_new_title">Ajouter une image au partenaire</h1>

            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-success mb-5">{{ button_label|default('Ajouter une image') }}</button>
                <a class="text-white ml-1 row" href="{{ path('home_index') }}">Revenir à l'accueil</a>
            {{ form_end(form) }}

        </div>
    </section>

{% endblock %}
