{% extends 'base.html.twig' %}

{% block title %}New Event's Poster{% endblock %}

{% block body %}
    <section class="poster_page_new text-white">
        <div class="poster_show_container container">
            <h1 class="poster_page_new_title">Ajouter une image à l'évèvenement</h1>
           <div class="event_form">
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
                {{ form_end(form) }}
            </div>
            <div class="mt-3">
                <a class="text-white ml-1 row" href="{{ path('home_index') }}">Revenir à l'accueil</a>
            </div>
        </div>
    </section>

{% endblock %}
