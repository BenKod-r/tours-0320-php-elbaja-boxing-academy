{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
    <section class="event_page_new text-white">
        <h1 class="event_page_new_title">Création d'un nouvel évènement</h1>
        <div class="event_show_container container">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-success mb-5">{{ button_label|default('Ajouter une image') }}</button>
            {{ form_end(form) }}
            <div class="mt-3">
                <a class="text-white" href="{{ path('event_index') }}">Retour</a>
            </div>
        </div>
    </section>
{% endblock %}
